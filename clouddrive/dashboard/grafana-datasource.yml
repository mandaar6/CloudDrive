apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    uid: loki
    isDefault: true
    version: 1
    editable: true 
    jsonData:
      maxLines: 1000

      # editable: false -- in first version. Changed after perplexity edit
